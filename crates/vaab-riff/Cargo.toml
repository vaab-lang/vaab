[package]
name = "vaab-riff"
description = "Riffs: Vaab's package manifest, lockfile, gather, and link."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
vaab-syntax = { path = "../vaab-syntax" }

[dev-dependencies]
insta.workspace = true
