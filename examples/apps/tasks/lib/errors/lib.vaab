# App-level errors. Stdlib errors (EnvError, DbError, AuthError, HttpError)
# are built-in — only declare what is yours.

choice ApiError {
    Unauthorized
    BadRequest(message: Text)
    Database(message: Text)
    Upstream(message: Text)
}
