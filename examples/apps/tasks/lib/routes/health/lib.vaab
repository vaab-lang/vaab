cast HealthRoute {
    factory handle() returns HealthStatus or fails ApiError {
        let db = try Database.open()
        try Database.ensure_schema(db)
        return HealthStatus.new(status: "ok", database: Database.path())
    }
}
