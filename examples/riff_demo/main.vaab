# Demo: installed riffs.
#
#   riff install supabase
#   cd examples/riff_demo
#   vaab need supabase
#   vaab run main.vaab
#
# `HttpError` is built-in.

need supabase

main() {
    let client = supabase.connect_with("https://example.supabase.co", "demo-key")
    print("supabase client for {client.url}")
}

main()
