choice HttpError {
    Failed(message: Text)
}

need supabase from "../../../vaab-riffs/supabase"

to main() {
    let client = supabase.connect_with("https://example.supabase.co", "demo-key")
    print("supabase client for {client.url}")
}

main()
